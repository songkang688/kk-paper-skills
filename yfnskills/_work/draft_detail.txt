=========== SENTENCES OVER 40 WORDS (with tex line no.) ===========
L52   [ 45 w] We define an evaluation protocol for separated-alpha recovery on 52 , 700 synthetic composites and union-mask generalization on 884 real images; no tr...
L72   [ 49 w] Smoke and fire perception has been dominated by binary semantic segmentation, with specialized designs for semi-transparency, blurry boundaries, and t...
L72   [ 45 w] Image matting instead predicts continuous per-pixel opacity, from deep image matting [REF] to trimap-free portrait matting [REF]; SFMattingNet targets...
L87   [ 44 w] Our working hypothesis is that the mask-oriented character of the frozen model must be corrected at two stages: feature formation, where the PixelDeco...
L87   [ 55 w] On the feature side, SF-TAB-Freq-LoRA equips the PixelDecoder convolutions with a task-switchable low-rank residual branch [REF], so that two forward ...
L90   [ 97 w] Our contributions are two modules and one protocol: (1) SF-TAB-Freq-LoRA, a task-conditioned parameter-efficient adaptation of the frozen SAM 3 PixelD...
L102  [ 45 w] SF-TAB-Freq-LoRA wraps the PixelDecoder convolutions of the frozen SAM 3; two task-conditioned forward passes with smoke and fire prompts produce per-...
L109  [ 60 w] On the feature side, SF-TAB-Freq-LoRA injects a shared low-rank residual branch into the PixelDecoder convolutions; before each prompt pass, all wrapp...
L119  [ 49 w] A local-detail gate re-weights the residual by the detail content of the input: the channel-mean energy map is smoothed by 33 average pooling, and the...
L139  [ 63 w] Stage k receives the projected decoder feature, the cue tensor, and the current probability and uncertainty states (plus the gated flame context in th...
L144  [ 41 w] The class-specific shaping matches the two media: the smoke branch blends a 33 average-pooled residual with the raw residual (0.65/0.35) to favor diff...
L159  [ 64 w] where, per task t , L _ grad ^ t matches finite-difference alpha gradients, L _ smooth ^ t enforces background smoothness with a weak foreground detai...
L159  [ 69 w] Both modules are implemented as standalone components, and the repository provides a unified training and evaluation entry (with an accompanying unit-...
L223  [ 49 w] For union evaluation, the predicted alphas are merged by a function g and threshold selected once on the synthetic validation split (over three candid...
L228  [ 67 w] The reference union mask on the validation split is derived from the ground-truth alphas with a fixed binarization constant (0.5, a protocol constant ...
L230  [ 73 w] Training seeds are pre-registered as 42, 43, 44 : every configuration is first run with seed 42, and a planned multi-seed tier repeats Table REF rows ...
L230  [ 51 w] All baselines follow the same splits, input resolution, augmentation, training budget, and evaluation scripts; dual-head baselines keep their original...
L237  [ 51 w] Table REF reports the mean squared error (MSE), the sum of absolute differences (SAD), the gradient error (Grad), and the transition-region mean absol...
L237  [ 44 w] T-MAE is computed per class on the pixels whose own ground-truth alpha lies in (0.05, 0.95) , not on a union transition region; images without such tr...
L237  [ 43 w] All entries are XX pending the first training run; once filled in, the analysis will report method differences, the error composition of the two subta...
L241  [ 84 w] Baseline adaptation: MODNet is re-trained with a shared encoder and two smoke/flame heads; SFMattingNet follows the same dual-head protocol if its ori...
L279  [125 w] Table REF isolates the two modules and the directed context through the training entry's configuration switches: row 1 uses P_t = (L_t) directly as al...
L279  [ 52 w] FPS and latency will be measured on a single GPU within one session, at batch size 1, the shared evaluation resolution, and fp32, with warm-up before ...
L299  [ 42 w] We presented a parameter-efficient framework that adapts a frozen SAM 3 to recover separated smoke and flame alpha mattes through task-conditioned fea...

=========== LONGEST SENTENCE PER SECTION ===========
Introduction                 longest  97 w at L90
Method                       longest  69 w at L159
Conclusion and Limitations   longest  42 w at L299
Abstract                     longest  45 w at L52
Experiments                  longest 125 w at L279
PREAMBLE                     longest  21 w at L42

=========== WHERE PURPOSE CLAUSES COULD BE FRONTED ===========
(sentences that state a mechanism first and bury the purpose in a subordinate clause)
L52   [ 35 w] On the feature side, SF-TAB-Freq-LoRA injects a shared low-rank residual branch into the PixelDecoder convolutions, conditioned per task by ...
L64   [ 40 w] The two categories also call for separate treatment: smoke spreads as soft diffuse layers, flame shows sharp high-frequency boundaries, and ...
L87   [ 55 w] On the feature side, SF-TAB-Freq-LoRA equips the PixelDecoder convolutions with a task-switchable low-rank residual branch [REF], so that tw...
L109  [ 23 w] The two mattes are predicted as separate per-class outputs, so a pixel covered by both media can carry non-zero opacity in both maps....
L109  [ 60 w] On the feature side, SF-TAB-Freq-LoRA injects a shared low-rank residual branch into the PixelDecoder convolutions; before each prompt pass,...
L114  [ 40 w] SF-TAB-Freq-LoRA wraps the standard (non-grouped) convolutions inside the frozen SAM 3 PixelDecoder with a low-rank residual branch: the dec...
L159  [ 69 w] Both modules are implemented as standalone components, and the repository provides a unified training and evaluation entry (with an accompan...
L279  [125 w] Table REF isolates the two modules and the directed context through the training entry's configuration switches: row 1 uses P_t = (L_t) dire...
L299  [ 18 w] First, the 884 real images provide only joint binary masks, so separated-alpha accuracy on real data cannot be verified....
L299  [ 38 w] Second, although the repository provides a unified training and evaluation entry, no end-to-end training run has been performed on the datas...

=========== INTRO DIFFICULTY SENTENCES (candidates for N-folds list) ===========
L64   [  9 w] Both media are semi-transparent, with diffuse, gradually fading boundaries.
L64   [ 20 w] Binary masks reduce every pixel to a hard in/out decision and discard how strongly the scene behind is occluded.
L64   [ 40 w] The two categories also call for separate treatment: smoke spreads as soft diffuse layers, flame shows sharp high-frequency boundaries, and they frequently over
L64   [ 31 w] Because real separated alpha ground truth for such volumetric media is prohibitively difficult to annotate, we train on synthetic composites with exact separate
L72   [ 49 w] Smoke and fire perception has been dominated by binary semantic segmentation, with specialized designs for semi-transparency, blurry boundaries, and thin early-
