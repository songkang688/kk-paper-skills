# 安装说明（自动生成，勿手工编辑）

本目录由安装流程从源包 yfnskills-1.7.0.zip 生成（macOS，等价复刻 _install_skill.ps1）。

与源目录的两处差异，都只在安装副本里：
1. 支撑目录 dna / playbook / evidence / corpus / _work 被平移成本目录的子目录，因此根目录文件里指向它们的上级路径前缀被去掉。
2. final_skill 里的文件被平铺到本目录根（Cursor 要求 SKILL.md 位于 skill 目录根），因此文件内的 final_skill 路径前缀被去掉。
